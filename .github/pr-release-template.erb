Release: <%= ENV.fetch('GIT_PR_RELEASE_BRANCH_STAGING') %> → <%= ENV.fetch('GIT_PR_RELEASE_BRANCH_PRODUCTION') %>: <%= Time.now %>

<% pull_requests.each do |pr| -%>
<%= pr.to_checklist_item %>
<% end -%>
